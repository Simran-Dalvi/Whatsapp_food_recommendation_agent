[project]
name = "whatsapp-food-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Simran", email = "simran.dalv30@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "openai>=2.40.0",
    "pandas>=3.0.3",
    "python-dotenv>=1.2.2",
]

[project.scripts]
whatsapp-food-agent = "whatsapp_food_agent:main"

[build-system]
requires = ["uv_build>=0.9.22,<0.10.0"]
build-backend = "uv_build"
